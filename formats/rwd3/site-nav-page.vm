## since this format is used independently of default, all library and setup formats must be imported
## a page tied with this format must use absolute links
#set( $core_site_name = "_common_assets" )
#import( "site://${core_site_name}/formats/library/velocity/chanw_library_import" )
#set( $master_site_name  = "_master_site" )
#import( "site://${master_site_name}/formats/${MASTER_SETUP_FORMAT_NAME}" )

#chanwImportScript( "formats/${design_namespace}" $master_site_name "site-nav-import" )
#createSiteNav( true $currentPageSiteName ) ## external view with PHP code